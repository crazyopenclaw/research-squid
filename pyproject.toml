[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "research-squid-workspace"
version = "0.2.0"
description = "Workspace metadata for the ResearchSquid monorepo."
requires-python = ">=3.12"

[tool.setuptools]
py-modules = []
